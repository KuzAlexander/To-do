<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.2/font/bootstrap-icons.css">
        {% endblock %}

        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
        {% endblock %}
        <style>
            .card-body__menu {
                display: flex;
                justify-content: space-between;
            }
            .todo-tab {
                /*margin-bottom: 20px;*/
            }
            .nav-item:hover .nav-link {
                background-color: #0d6efd;
                color: white;
            }
            .todo-list {
                display: flex;
                flex-direction: column;
            }
            .todo-item {
                display: flex;
                align-items: center;
                margin-bottom: 10px;
                padding: 5px 10px;
                border: 1px solid lightgray;
                border-radius: 10px;
            }
            .todo-item:hover{
                background-color: lightgray;
            }
            .todo-checkbox {
                height: 20px;
                width: 20px;
                border: 1px solid #0d6efd;
                border-radius: 5px;
                margin-right: 15px;
            }
            .todo-checkbox__fill {
                height: 20px;
                width: 20px;
                border: 1px solid #0d6efd;
                border-radius: 5px;
                margin-right: 15px;
                background-color: #0d6efd;
            }
            .todo-text {
                font-size: 20px;
                flex-grow: 1;
            }
            .todo-text__trough {
                text-decoration: line-through;
            }
            .bi-x-square {
                font-size: 20px;
                color: #0d6efd;
            }
            .bi-x-square:hover {
                color: red;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <h1>{{ project_title }}</h1>
            <div class="row">
                <div class="col-md-12">
                    <div class="card card-white">
                        <div class="card-body">
                            {#                    {{ form(form) }}#}
                            <div class="card-body__menu">
                                <ul class="nav nav-pills todo-tab">
                                    <li role="presentation" class="nav-item">
                                        <a href="{{ path('show_all_task') }}" class="nav-link">Все</a>
                                    </li>
                                    <li role="presentation" class="nav-item">
                                        <a href="{{ path('active_task') }}" class="nav-link">Активные</a>
                                    </li>
                                    <li role="presentation" class="nav-item">
                                        <a href="{{ path('made_task') }}" class="nav-link">Выполненные</a>
                                    </li>
                                </ul>
                                {% block button %}{% endblock %}
                            </div>
                            <hr>
                            {% block body %}{% endblock %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </body>
</html>
